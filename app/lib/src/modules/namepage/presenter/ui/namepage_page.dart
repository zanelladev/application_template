import 'package:flutter/material.dart';

class NamePagePage extends StatelessWidget {
  const NamePagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
